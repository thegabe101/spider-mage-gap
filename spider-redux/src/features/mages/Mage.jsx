import { useSelector } from "react-redux";
import { selectAllMages } from "./magesSlice";

const Mage = () => {
    console.log("dfda")

    let content = "mage"
    return (
        <section>
            {content}
        </section>
    )
}
export default Mage
